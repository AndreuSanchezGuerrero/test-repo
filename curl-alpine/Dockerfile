# Start from a small base image
FROM alpine:3.20

# Set a maintainer label (optional)
LABEL maintainer="you@example.com"

# Install a simple package (curl for testing)
RUN apk add --no-cache curl

# Default command
CMD ["echo", "Hello from Uyuni build!"]
